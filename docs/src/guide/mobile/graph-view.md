# グラフビュー

グラフビューは、未完了のタスクとその依存関係を有向グラフで表示する画面です。
有向グラフでは、タスクをノード、依存関係をエッジとして表します。
エッジには方向があり、依存元から依存先へ向かいます。

## 表示内容

- 未完了タスクをノードとして表示します。
- 依存関係をエッジとして表示します。
- 完了済みタスクは灰色で表示されます。

## 編集モード

上部の「編集」ボタンを押すと、エッジやノードを操作できます。

- **エッジを指で切る**：依存関係を削除
- **ノード間をドラッグ**：依存関係を追加
